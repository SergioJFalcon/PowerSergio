import { dev } from '$app/environment'

export const title = 'PowerSergio'
export const description = 'Personal climber site for tracking progress and training'
export const url = dev ? 'http://localhost:5173/' : 'https://powersergio.com/'
